version: "3.9"

services:
  app1:
    build:
      context: ./app1
    container_name: app1-container
    ports:
      - "3000:3000"

  app2:
    build:
      context: ./app2
    container_name: app2-container
    ports:
      - "3001:3001"